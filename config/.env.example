# =============================================================================
# AI Morning Brief - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values.
# Never commit your .env file to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# TWITTER_BEARER_TOKEN (required)
# -----------------------------------------------------------------------------
# Used to fetch recent tweets via Twitter API v2.
#
# How to get it:
# 1. Go to https://developer.twitter.com/en/portal/dashboard
# 2. Sign up for a developer account if you don't have one
# 3. Create a new Project and App (or use existing)
# 4. In your App settings, go to "Keys and tokens"
# 5. Generate a Bearer Token under "Authentication Tokens"
# 6. Copy the Bearer Token value below
#
# Note: Free tier allows 500k tweets/month read access.
TWITTER_BEARER_TOKEN=

# -----------------------------------------------------------------------------
# OPENAI_API_KEY (required)
# -----------------------------------------------------------------------------
# Used for content analysis, classification, and summary generation.
#
# How to get it:
# 1. Go to https://platform.openai.com/signup (create account if needed)
# 2. Navigate to https://platform.openai.com/api-keys
# 3. Click "Create new secret key"
# 4. Give it a name (e.g., "ai-morning-brief")
# 5. Copy the key immediately (it won't be shown again)
#
# Note: Requires a paid account with credits. Usage costs apply per token.
OPENAI_API_KEY=

# -----------------------------------------------------------------------------
# DISCORD_WEBHOOK_URL (required)
# -----------------------------------------------------------------------------
# Used to send the daily digest to a Discord channel.
#
# How to get it:
# 1. Open Discord and go to the server where you want to post
# 2. Right-click the target channel → "Edit Channel"
# 3. Go to "Integrations" → "Webhooks"
# 4. Click "New Webhook" (or use existing)
# 5. Customize the name/avatar if desired
# 6. Click "Copy Webhook URL"
#
# Format: https://discord.com/api/webhooks/{webhook_id}/{webhook_token}
DISCORD_WEBHOOK_URL=

# -----------------------------------------------------------------------------
# GITHUB_TOKEN (optional but recommended)
# -----------------------------------------------------------------------------
# Increases GitHub API rate limits from 60/hour to 5000/hour.
# Required if crawling many GitHub repos.
#
# How to get it:
# 1. Go to https://github.com/settings/tokens
# 2. Click "Generate new token" → "Generate new token (classic)"
# 3. Give it a name (e.g., "ai-morning-brief")
# 4. Select expiration (90 days recommended for security)
# 5. Select scopes: only "public_repo" is needed (or no scopes for public read)
# 6. Click "Generate token" and copy immediately
#
# Alternative: Use fine-grained tokens at https://github.com/settings/tokens?type=beta
# with "Public Repositories (read-only)" access.
GITHUB_TOKEN=
